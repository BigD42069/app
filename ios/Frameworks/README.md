# iOS native framework drop-in

Place the generated `ios.xcframework` directory from the gomobile build output inside this folder.

The Xcode project links and embeds `Frameworks/ios.xcframework`, so make sure the framework folder is checked into the repository or copied locally before building.
